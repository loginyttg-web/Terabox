[phases.setup]
nixPkgs = ["nodejs_22", "zip"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"

[variables]
NODE_ENV = "production"
HOST = "0.0.0.0"
